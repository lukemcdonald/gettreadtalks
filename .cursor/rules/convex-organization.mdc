---
description: Convex function organization and naming conventions
globs: convex/**/*.ts
---

# Convex Organization

## File Structure

```sh
convex/
├─ auth.config.ts      # Auth configuration
├─ auth.ts             # Authentication setup
├─ convex.config.ts    # Convex configuration
├─ convex.json         # Convex project config
├─ http.ts             # HTTP endpoints (webhooks)
├─ items.ts            # User favorites queries & mutations
├─ schema.ts           # Database schema
├─ users.ts            # User queries & mutations
├─ utils.ts            # General utility functions
├─ model/              # Entity-specific helper functions for queries, mutstaions and utils
│  ├─ index.ts         # Barrel exports
│  ├─ auth.ts          # Authentication helpers
│  ├─ items.ts         # Entity type specific helpers (example)
│  └─ users.ts         # User-specific helpers
└─ _generated/         # Auto-generated
```

## Naming Conventions

### Query Functions (reads)

Describe the data operation:

```ts
export const get = query(...)         // Get single by ID
export const getBySlug = query(...)   // Get by other field
export const list = query(...)        // Get collection
export const search = query(...)      // Filtered list
```

### Mutation Functions (writes)

Use action verbs:

```ts
export const archive = mutation(...)  // Specific action
export const create = mutation(...)   // Create new
export const publish = mutation(...)  // Specific action
export const remove = mutation(...)   // Delete (avoid "delete" keyword)
export const toggle = mutation(...)   // Toggle state
export const update = mutation(...)   // Update existing
```

### Function Naming Examples

```ts
// ✅ Clear
export const getBySlug = query(...)
export const list = query(...)

// ❌ Vague
export const data = query(...)
export const stuff = query(...)

// ✅ Concise
export const get = query(...)            // api.items.get
export const list = query(...)           // api.items.list

// ❌ Redundant (file provides context)
export const getItem = query(...)        // in items.ts
export const listItems = query(...)      // in items.ts
```

## Database Schema Conventions

### Field Naming

Name fields by what they reference:

```ts
items: defineTable({
  categoryId: v.id('categories'),
  statusId: v.id('statuses'),
  userId: v.id('user'), // or authorId, be consistent
});
```

### Index Naming

Name indexes by what you're querying:

```ts
.index('by_category', ['categoryId'])
.index('by_status', ['statusId'])
.index('by_user', ['userId'])
.index('by_user_and_category', ['userId', 'categoryId'])
```

### Consistency

Stay consistent within each table:

- `userId` + `by_user` OR `authorId` + `by_author`
- Don't mix naming styles

## Function Organization

### Group by Domain

One file per domain/entity:

```ts
// convex/items.ts
export const create = mutation(...)
export const get = query(...)
export const list = query(...)
export const remove = mutation(...)
export const update = mutation(...)

// convex/topics.ts
export const get = query(...)
export const getBySlug = query(...)
export const list = query(...)
```

### Entity-Specific Helpers in model/

Organize entity-specific logic in the `model/` directory:

```ts
// convex/model/auth.ts - Cross-cutting authentication
export async function getCurrentUser(ctx: QueryCtx | MutationCtx) {
  const user = await authComponent.getAuthUser(ctx);
  return user || null;
}

// convex/model/talks.ts - Talk-specific helpers
export async function getPublishedWithSpeakers(
  ctx: DatabaseReader,
  limit: number,
): Promise<Array<Doc<'talks'> & { speaker: Doc<'speakers'> | null }>> {
  const talks = await ctx
    .query('talks')
    .withIndex('by_status_and_published_at', (q) => q.eq('status', 'published'))
    .order('desc')
    .take(limit);

  return Promise.all(
    talks.map(async (talk) => {
      const speaker = await ctx.db.get(talk.speakerId);
      return { ...talk, speaker };
    }),
  );
}

// convex/model/clips.ts - Clip-specific helpers
export async function getBySlugWithRelations(
  ctx: DatabaseReader,
  slug: string,
  topicLimit: number = 20,
): Promise<{
  clip: Doc<'clips'>;
  speaker: Doc<'speakers'> | null;
  talk: Doc<'talks'> | null;
  topics: Doc<'topics'>[];
} | null> {
  // Implementation for clip with relations
}

// convex/utils.ts - General utilities (not entity-specific)
export function normalizeSlug(text: string): string {
  return text.toLowerCase().replace(/[^a-z0-9\s-]/g, '').replace(/\s+/g, '-');
}
```

## Authorization Pattern

Always check auth in mutations using model helpers:

```ts
// ✅ Use model helpers for consistent auth
import { requireAuth, getUserId } from './model';

export const update = mutation({
  handler: async (ctx, args) => {
    const userId = await getUserId(ctx); // Throws if not authenticated
    
    const resource = await ctx.db.get(args.id);
    if (resource.userId !== userId) {
      throw new Error('Not authorized');
    }

    // Now safe to update
  },
});
```

## Data Enrichment Pattern

Enrich data with relationships using model helpers:

```ts
import { getBySlug, getClipTopics } from './model';

export const getBySlug = query({
  handler: async (ctx, args) => {
    const clip = await getBySlug(ctx.db, 'clips', args.slug, 'published');
    if (!clip) return null;

    // Use helper functions for related data
    const [speaker, talk, topics] = await Promise.all([
      clip.speakerId ? ctx.db.get(clip.speakerId) : null,
      clip.talkId ? ctx.db.get(clip.talkId) : null,
      getClipTopics(ctx.db, clip._id, 20),
    ]);

    return { clip, speaker, talk, topics };
  },
});
```

## Model Directory Structure

The `model/` directory contains **entity-specific helper functions** organized by domain:

### Authentication (`model/auth.ts`) - Cross-cutting
- `getCurrentUser()` - Get authenticated user or null
- `requireAuth()` - Require authentication, throw if not authenticated  
- `getUserId()` - Get user ID, throw if not authenticated

### Entity-Specific Helpers
- **`model/talks.ts`** - Talk-specific queries and operations
- **`model/clips.ts`** - Clip-specific queries and operations
- **`model/topics.ts`** - Topic-specific queries and operations
- **`model/collections.ts`** - Collection-specific queries and operations
- **`model/speakers.ts`** - Speaker-specific queries and operations
- **`model/users.ts`** - User-specific queries and operations

### General Utilities (`utils.ts`) - Root level
- `normalizeSlug()` - URL-friendly slug generation
- `slugExists()` - Check if slug exists in table
- `getDefaultLimit()` - Get appropriate default limits by content type

### Usage Pattern
```ts
// Import entity-specific helpers from model
import { getPublishedWithSpeakers } from './model/talks';
import { getBySlugWithRelations } from './model/clips';
import { getCurrentUser } from './model';
import { normalizeSlug } from './utils';

// Use in functions
export const getPublished = query({
  handler: async (ctx, args) => {
    const limit = args.limit || 20;
    return await getPublishedWithSpeakers(ctx.db, limit);
  },
});
```

## Best Practices

### Avoid Redundant Indexes

```ts
// ❌ Redundant - only need the second one
.index('by_user', ['userId'])
.index('by_user_and_category', ['userId', 'categoryId'])

// ✅ Only define what you need
.index('by_user_and_category', ['userId', 'categoryId'])
.index('by_category', ['categoryId'])
```

Use the more specific index for all queries, even when you don't need all fields.

### Use Entity-Specific Model Helpers

```ts
// ✅ Use entity-specific model helpers
import { getPublishedWithSpeakers } from './model/talks';
import { getBySlugWithRelations } from './model/clips';
import { getDefaultLimit } from './utils';

export const getPublished = query({
  handler: async (ctx, args) => {
    const limit = args.limit || getDefaultLimit('content');
    return await getPublishedWithSpeakers(ctx.db, limit);
  },
});

// ❌ Don't duplicate entity-specific logic
export const getPublished = query({
  handler: async (ctx, args) => {
    const limit = args.limit || 50; // Duplicated default
    const talks = await ctx.db
      .query('talks')
      .withIndex('by_status_and_published_at', (q) => q.eq('status', 'published'))
      .order('desc')
      .take(limit); // Duplicated query pattern
    
    // Duplicated speaker fetching logic
    return Promise.all(talks.map(async (talk) => {
      const speaker = await ctx.db.get(talk.speakerId);
      return { ...talk, speaker };
    }));
  },
});
```
